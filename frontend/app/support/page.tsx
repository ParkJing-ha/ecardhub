import InfoPage from "../../components/InfoPage";

export default function SupportPage() {
  return (
    <InfoPage eyebrow="Support" title="Support">
      <p>
        Need help with accounts, cards, payments, guests, or QR verification?
        Email support@invitely.local and include your event name.
      </p>
    </InfoPage>
  );
}
